import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const PracticeExamListScreen = () => {
  return (
    <View>
      <Text>PracticeExamListScreen</Text>
    </View>
  );
};

export default PracticeExamListScreen;

const styles = StyleSheet.create({});
